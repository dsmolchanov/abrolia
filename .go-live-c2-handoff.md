# Go-live C1+C2 — handoff (2026-08-24)

## State

- **Branch `codex/go-live-c1-c2`** — four commits on top of `c7e1d36`
  (origin/main, which is the #70 squash; tree-identical to the phase-F branch
  tip, so nothing was lost by rebasing onto main):
  - `9b989cf` docs(privacy): executed vendor DPAs + Fly.io DPA route
  - `9023a1a` feat(cost-caps): guard and record every model call at its call site
  - `063f72a` feat(web-chat): runtime serves chat over the internal bearer boundary
  - `2c99080` docs(plans): go-live checklist + canon validation report
- **Push status: NOT CONFIRMED.** The user asked to push; every attempt this
  session hit a classifier outage ("stealth/ox-alpha is temporarily
  unavailable"). First action next session: `git push -u origin
  codex/go-live-c1-c2`, then verify with `git ls-remote origin
  codex/go-live-c1-c2`. No PR opened yet (not requested).
- The old branch `codex/phase-F-allowlist-at-dispatch` (8 commits) is
  untouched and fully contained in #70's squash.
- Plan doc with full execution log:
  `thoughts/shared/plans/2026-08-23-go-live-checklist.md` — Track O/C/R state,
  C-slice order, execution log through C2.

## What landed

1. **C1 cost caps** — `is_over_budget` guards WhatsApp dialogue
   (`runner/pipeline.py`), Telegram dialogue, and web channel
   (`channels/web.py`) before each model call; tokens recorded after;
   over-budget answers DEGRADED_MESSAGE; `budget_exceeded` emitted via new
   `emit_alert` (unknown alert names raise). Tests in `tests/test_cost_caps.py`.
2. **C2 seam — runtime serves, control plane proxies.**
   - Runtime: `POST /internal/v1/web/chat` inside `INTERNAL_ROUTES`
     (`runtime/service.py`) → hoisted bearer gate covers it structurally
     (unconfigured token → 404 hides route; wrong bearer → 401).
     `_web_chat`: text validation (`text_required`/`text_too_long`),
     owner-only fail-close (`owner_role_required`). `web_chat_turn`: first
     in-runtime ToolLoop — `require_ready()` + explicit
     `load_config(manifest_path=self.manifest_path)`; loop+UsageStore rebuilt
     per turn inside one `open_database` block (EffectJournal binds a
     connection); cap via `HERMES_COST_CAP_USD_PER_DAY` (default $5).
   - Control plane: `PrivateRuntimeWebChatClient`
     (`control_plane/runtimes/chat_client.py`) proxies over
     `{runtime_ref}.internal:8080`, same `runtime-dsar:{ref}` bearer as DSAR;
     `/api/web/message` behind `require_private_mutation` (origin → session →
     CSRF double-submit), honest 503s when unprovisioned/unreachable; echo
     fallback deleted. PWA `app.js` sends the CSRF header.
3. **O1 legal evidence** — Anthropic DPA + Resend DPA verbatim snapshots under
   `docs/privacy/vendor-dpas/`; registry rows P1/P4 ✅. **P2 Fly.io stays ⏳**:
   no public customer DPA exists (checked 2026-08-23); route = written request
   to their support («Privacy Concerns»); DPF ≠ Art 28(3) substitute.

## Verification at commit time

Full non-live suite: 1484 tests, `[100%]`, exit 0 (= prior 1475 + 4 endpoint
cases + 5 runtime cases). Ruff clean; fixture sanitizer clean.

## Design decisions C3 must respect (locked in C2b)

- Web chat is owner-only until an account→actor binding lifecycle exists;
  effects attribute to `manifest.actors.owner`.
- Household is built WITHOUT manifest `verified_bindings` on purpose: the
  bearer boundary IS the transport verification while verified pairs cover
  telegram only. **C3 moves web into the manifest's verified pairs** — after
  that, building it manually becomes the bug.
- The control plane never grows a model loop; chat follows the DSAR proxy
  precedent.

## Next session playbook

1. Push + verify remote head = `2c99080` (see State).
2. Remaining slices in dependency order: **C3 binding lifecycle** (challenge →
   verify → row write; lift `synthetic-`-only external IDs with per-channel
   format validation; second-adult flow — planner hardcodes `family=(owner,)`,
   `provisioning/planner.py:137-146`), then C4 preferences, C5 gateway, C6 box
   hygiene.
3. Operator items that only a human can do (Track O): send the Fly.io DPA
   request email; three TIAs; Art 27 representative mandate (self-appointment
   ruled out); O2 release tag + restore drill; R0 allowlist prep.

## Environment notes

- **Classifier outages persist THIS session**: write-shaped tool calls
  intermittently fail ("temporarily unavailable"); retry with spacing works.
  `~/.claude/settings.local.json` allowlists (`git push:*`, `gh pr view:*`,
  …) load only in NEW sessions.
- pytest 9 `-q | tail` drops the verdict line through pipes (this is why the
  suite output ends at "-- Docs:") — trust `${pipestatus[1]}` / exit code and
  count progress dots.
- venv `.venv/bin/python`; ruff 0.16.4.
- Generation-corruption hazard seen twice today when writing large Python in
  one Write late in long turns: visually inspect generated files before
  running; keep rewrites short.

## Housekeeping (user's call)

Untracked scratch, all deletable when no longer wanted:
`.phase-b-delete-operator.py`, `.phase-b-runtime-operator.py`,
`.phase-b-tamper.py`, `.phase-f-handoff.md`, `.phase-f-reply.md`, and this
file.

## Hard rules in force (do not violate)

Never force-push; never amend merged commits; never touch AGENTS.md,
CLAUDE.md, `.github/workflows/codex-review-window.yml`, or branch protection
in fix commits; synthetic-only data until explicit gates (B-07 open — P2
unsigned, TIAs undone); registry dates ONLY AFTER signature; secrets never in
argv / DB / logs / manifests.
