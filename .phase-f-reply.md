## Round-nine blockers — answered at 6591143

Both inline blockers on `a0ac5f3` are addressed by 6591143 (`fix(provisioning): exhaustive reconcile dispatch and the proven erasure sequence`). Design pass and execution log: `thoughts/shared/plans/2026-08-22-phase-F-closure-design-pass.md`.

### [BLOCKER] Finish deletion-owned email cleanup (worker.py `_reconcile`, id 3835852507)

The premise — "deletion never makes that transition" — is stale against `a0ac5f3`: that commit is precisely the fix ("fix(delete): enter the disconnect lifecycle so erasure's own cleanup can finish"). `DeletionService.delete` marks every household email identity `disconnecting` inside the same durable transaction that writes `households.status = 'deleting'` (`control_plane/privacy/delete.py:209`), so any job deletion owns already satisfies the precondition `_cleanup`'s secret deletion requires.

Rather than argue staleness, we proved it. The new sequence test drives all nine cases — 3 providers × 3 origins of ambiguity (braked, pre-quarantined by withdrawal, ambiguous only after the deletion transaction) — through origin → delete → reconcile → derived-reference teardown → cleanup → parent settlement → resume → household row gone, with the real worker and the real `DeletionService`. It was green on first execution against `a0ac5f3`. Mutation M1 in `.phase-f-mutations.py` reverts exactly the transition this finding says is missing; under it the sequence test goes red. All four mutations killed.

### [BLOCKER] Exercise the cleanup to a terminal state (test:1299, id 3835867859)

Landed as `test_erasure_sequence_settles_every_job_and_completes_deletion` (`tests/control_plane/test_real_email_wiring.py:1312`) — precisely the demanded shape: an ambiguous parent job tied to the identity, deletion held open, reconciliation through `_shutdown_probe` deriving the teardown reference (the request carries no external_ref — the timeout-without-answer case), deprovision returning ABSENT, `_cleanup` settling succeeded with resource `deleted`, `_settle_cancelled_parent` settling the original job `cancelled_and_compensated`, the identity reaching terminal state via `finish_disconnect`, and `DeletionService.resume` completing — household row deleted — for `google-oauth`, `nerve-managed`, and `nerve-byo-domain`.

### Also closed: the residue class both rounds circled

The route existed because `_reconcile` dispatched by kind but gated its email branch on adapter shape (`getattr(provider, "reconcile", None)`), with an unguarded tail whose first act was `provider.inspect` — a mutating recovery path on every real adapter. 6591143 hoists the contract: shutdown routing sits above every adapter-shaped check; forward reconcile REQUIRES `reconcile` and fails closed with `failed/provider_cannot_reconcile`; every schema kind is handled explicitly and the tail is now a fail-closed `outcome_unknown/reconcile_unsupported` refusal; the `Provisioner` protocol declares `reconcile`. Pins that asserted the deleted tail's own semantics were renegotiated onto the contract (recorded in the plan doc), `AGENTS.repo-invariants.md` records the rule, and mutations M2–M4 kill the hoist's reintroductions.

The two remaining round-nine P1s (delete.py lifecycle entry; worker.py lease) were answered by e9168d1 / cadbd57 / a0ac5f3 before this generation.

At 6591143: full non-live suite green, ruff clean.
