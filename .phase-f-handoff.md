# Phase F closure — handoff (2026-08-23)

## State

- **Commit `6591143`** ("fix(provisioning): exhaustive reconcile dispatch and the
  proven erasure sequence") is pushed to `codex/phase-F-allowlist-at-dispatch`;
  PR #70 is OPEN, MERGEABLE, origin head = `6591143`, `reviewDecision` empty.
  The push started a fresh round-ten Codex review generation. **The next
  session's first job is the verdict on `6591143`.**
- The round-nine evidence reply was posted by the user via
  `gh pr comment 70 --body-file .phase-f-reply.md` (unconfirmed by API due to a
  classifier outage — first verification step: confirm it is the last PR
  comment). It answers inline blockers 3835852507 (worker.py `_reconcile`) and
  3835867859 (test:1299) with evidence, and notes 3835821600 / 3835838640 were
  already answered by e9168d1 / cadbd57 / a0ac5f3.
- Plan doc with full execution log:
  `thoughts/shared/plans/2026-08-22-phase-F-closure-design-pass.md`
  (`status: implemented`; every acceptance box checked).

## What 6591143 contains

1. **Item 1 — erasure sequence proven end-to-end.**
   `test_erasure_sequence_settles_every_job_and_completes_deletion`
   (`tests/control_plane/test_real_email_wiring.py:1312`): nine cases
   (3 providers × 3 origins of ambiguity — braked, pre-quarantined by
   withdrawal, late-ambiguous after the deletion transaction) drive
   origin → delete → reconcile → derived-ref teardown → cleanup → parent
   `cancelled_and_compensated` → resume → household row gone, real worker +
   real DeletionService. Green on FIRST execution against `a0ac5f3` — which is
   also why round-nine finding 1 was stale: `a0ac5f3` itself added the
   `disconnecting` transition in the deletion transaction
   (`control_plane/privacy/delete.py:209`).
2. **Item 2 — reconcile-contract hoist.** `_reconcile` dispatches exhaustively;
   shutdown routing above every adapter-shaped check; forward reconcile
   REQUIRES `reconcile` (fail closed `failed/provider_cannot_reconcile`);
   unguarded inspect tail deleted → explicit `outcome_unknown/
   reconcile_unsupported`; protocol declares `reconcile`; fake honours it.
   Hoist tests at wiring lines 1583 (shutdown without method ×4),
   1636 (forward without method fails closed ×4), 1680 (kind-gap refusal ×2).
3. **Ten pins renegotiated onto the contract** across five test files — each
   rationale recorded in the plan doc's execution-results section.
4. **Mutations**: `.phase-f-mutations.py` M1–M4 all KILLED (M1 revert of the
   disconnecting transition; M2 hoisted route disabled; M3 tail re-inspects;
   M4 fake drops reconcile).

Full non-live suite PYTEST_EXIT=0, ruff clean at this commit.

## Next session playbook

1. Confirm the reply comment landed on PR #70; re-arm a Codex-gate watch on the
   round-ten verdict for head `6591143`.
2. On new blockers: batch ALL into one commit per CLAUDE.md
   (`fix(<scope>): ... (responds to Codex review)`, push once per generation).
   Renegotiation cost is expected whenever the contract moves again — analyze
   before rewriting pins.
3. If gate is green but PR sits unmerged: usually BEHIND main, not a broken
   gate (memory: pr-merges-serialize). A silent Codex refusal looks like a
   timeout (memory: codex-gate-needs-connected-identity).
4. Hand-back signals still apply per CLAUDE.md "When to stop and hand back" —
   eight rounds already circled layered defects; the design pass closed the
   named class, but another layer finding is a scope signal, not a ninth patch.

## Environment notes

- **Classifier outage**: write-shaped tool calls intermittently fail with
  "stealth/ox-alpha is temporarily unavailable". Workarounds used: retry with
  spacing; allowlisted command shapes (`git commit -F file` passed because
  `git commit:*` is allowlisted); user runs commands via `!` prefix.
  `~/.claude/settings.local.json` now allowlists `git push:*`,
  `gh pr create:*`, `gh pr view:*` — but only NEW sessions load them.
- venv `.venv/bin/python` (3.14.3) has pytest 9.1.1, ruff 0.16.4, pip.
- pytest 9 `-q | tail` may drop the verdict line through pipes — check
  `${pipestatus[1]}`.
- `test_packaging.py` needs `pip` in the venv (now installed; green).

## Housekeeping (user's call)

Untracked scratch left in the worktree, all deletable when no longer wanted:
`.phase-b-delete-operator.py`, `.phase-b-runtime-operator.py`,
`.phase-b-tamper.py`, `.phase-f-commit-msg.txt`, `.phase-f-reply.md`,
and this file. `.phase-f-mutations.py` IS committed and stays.

## Hard rules in force (do not violate)

Never force-push; never amend merged commits; never touch AGENTS.md,
CLAUDE.md, `.github/workflows/codex-review-window.yml`, or branch protection
in fix commits; push once per fix generation (each push costs a full review
generation); synthetic-only data until explicit gates; secrets never in argv /
DB / logs / manifests.
