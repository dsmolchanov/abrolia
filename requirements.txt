# Рантайм household-инстанса. Версии пинуются минимальной границей: SDK
# Anthropic до 0.116 не умеет messages.parse (структурированный вывод), а на
# нём держится весь контракт извлечения.
anthropic>=0.120,<1
pydantic>=2.9,<3
python-dotenv>=1.0,<2
# Шифрование бэкапов (AES-256-GCM). Тянется и транзитивно, но бэкап зависит от
# него напрямую — значит, зависимость объявлена явно.
cryptography>=43,<46
